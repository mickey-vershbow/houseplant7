<!DOCTYPE html>
<html lang="en">
  <!-- THE HEAD TAG PARTIAL -->
  <%- include("../partials/head") %>
  <body>
    <!-- THE HEADER TAG PARTIAL -->
    <%- include("../partials/header") %>

    <!-- DISPLAY OUR PLANTS -->

    <section class="hero is-header has-text-centered">
      <div class="hero-body">
        <p class="title">Your Personal Plant Collection </p>
        <p class="subtitle">
          <!-- Add new plant button -->
          <a class="button new-plant-button" href="/user/new">Add A New Plant</a>
        </p>
      </div>
    </section>

    <main class="profile-container">
      <div class="container is-fluid profile-plants">
        <% for(let i = 0; i < plants.length; i++) { %>
          <br>

      <figure class="image is-128x128 profile-plants-links">
        <!-- SHOW PAGE -->
        <a href="/user/profile/<%=plants[i]._id;%>">
          <h1 class="plant-name has-text-centered"><%= plants[i].name %></h1>
          <img src="<%= plants[i].url %>" />
      </a>
       <!-- DELETE FORM -->
              <form action="/user/profile/<%=plants[i]._id; %>?_method=DELETE" method="POST" class="delete-button">
                <input type="submit" value="Delete Plant" class="button is-small is-danger is-outlined"/>
              </form>
    <% } %>
      </figure>
      <br><br><br><br>
    </div>


    <!-- Log out button -->
    <!-- <a class="button logout-button" href="/user/logout">Log Out</a> -->

    </main>

    <!-- THE FOOTER PARTIAL -->
    <%- include("../partials/footer") %>


  </body>
</html>
